proc str_len (ptr -> int)
  proc str_len_go (ptr int -> ptr int)
    dup read1 0 eqi not if
      1 add
      swap
      1 add
      swap
      str_len_go
    end
  end
  0 swap str_len_go pop
end

proc print_null_term_str (ptr -> )
  dup str_len prints
end

"XXX - DEF"
dup       65 swap write1
dup 1 add 66 swap write1
dup 2 add 67 swap write1
print_null_term_str